<%= form_with(model: field_type) do |form| %>
  <% if field_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(field_type.errors.count, "error") %> prohibited this field_type from being saved:</h2>

      <ul>
        <% field_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :kind, style: "display: block" %>
    <%= form.select :kind, FieldType.kinds.keys %>
  </div>

  <div>
    <%= form.label :template_id, style: "display: block" %>
    <%= form.text_field :template_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
